
#include <vector>

#include "matmul.h"

using namespace std;
int main() {
  int m, k, n;

  vector<vector<int>> test_params = {
      {1, 1, 1}, {128, 128, 128}, {3, 4, 5}, {2, 2, 3}};
  for (auto params : test_params) {
  }
}